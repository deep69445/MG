<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Shopify Growth Studio — Premium Shopify Agency</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;500;600;700;800&family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;1,9..40,300&display=swap" rel="stylesheet">
<style>
  :root {
    --bg: #0a0a0a;
    --bg2: #111111;
    --bg3: #161616;
    --card: #141414;
    --border: rgba(255,255,255,0.07);
    --border2: rgba(255,255,255,0.12);
    --text: #f0ede8;
    --muted: #7a7570;
    --muted2: #4a4845;
    --accent: #6C63FF;
    --accent2: #9c96ff;
    --accent-dim: rgba(108,99,255,0.12);
    --accent-dim2: rgba(108,99,255,0.06);
    --white: #ffffff;
    --radius: 16px;
    --radius-sm: 8px;
    --font-display: 'Syne', sans-serif;
    --font-body: 'DM Sans', sans-serif;
  }

  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  html { scroll-behavior: smooth; }

  body {
    background: var(--bg);
    color: var(--text);
    font-family: var(--font-body);
    font-size: 16px;
    line-height: 1.65;
    overflow-x: hidden;
  }

  /* ── NAV ── */
  nav {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 100;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0 48px;
    height: 68px;
    background: rgba(10,10,10,0.85);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    border-bottom: 1px solid var(--border);
  }

  .nav-logo {
    font-family: var(--font-display);
    font-weight: 800;
    font-size: 18px;
    letter-spacing: -0.5px;
    color: var(--white);
    text-decoration: none;
  }

  .nav-logo span { color: var(--accent2); }

  .nav-links {
    display: flex;
    align-items: center;
    gap: 36px;
    list-style: none;
  }

  .nav-links a {
    font-size: 14px;
    font-weight: 400;
    color: var(--muted);
    text-decoration: none;
    letter-spacing: 0.2px;
    transition: color 0.2s;
  }

  .nav-links a:hover { color: var(--text); }

  .nav-cta {
    background: var(--accent);
    color: #fff !important;
    padding: 8px 20px;
    border-radius: 6px;
    font-weight: 500 !important;
    font-size: 14px !important;
    transition: background 0.2s, opacity 0.2s !important;
  }

  .nav-cta:hover { background: var(--accent2) !important; color: #fff !important; opacity: 1 !important; }

  /* ── HERO ── */
  .hero {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 120px 24px 80px;
    position: relative;
    overflow: hidden;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    pointer-events: none;
    overflow: hidden;
  }

  .hero-orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    opacity: 0.35;
  }

  .orb1 {
    width: 600px; height: 600px;
    background: radial-gradient(circle, #6C63FF 0%, transparent 70%);
    top: -200px; left: -100px;
    animation: drift1 12s ease-in-out infinite;
  }

  .orb2 {
    width: 500px; height: 500px;
    background: radial-gradient(circle, #a78bfa 0%, transparent 70%);
    bottom: -150px; right: -100px;
    animation: drift2 15s ease-in-out infinite;
  }

  .orb3 {
    width: 300px; height: 300px;
    background: radial-gradient(circle, #818cf8 0%, transparent 70%);
    top: 50%; left: 50%;
    transform: translate(-50%, -50%);
    animation: drift3 10s ease-in-out infinite;
  }

  @keyframes drift1 {
    0%, 100% { transform: translate(0,0); }
    50% { transform: translate(60px, 40px); }
  }
  @keyframes drift2 {
    0%, 100% { transform: translate(0,0); }
    50% { transform: translate(-50px, -30px); }
  }
  @keyframes drift3 {
    0%, 100% { transform: translate(-50%, -50%) scale(1); }
    50% { transform: translate(-50%, -50%) scale(1.2); }
  }

  .hero-grid-overlay {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(255,255,255,0.02) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255,255,255,0.02) 1px, transparent 1px);
    background-size: 60px 60px;
  }

  .hero-eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: var(--accent-dim);
    border: 1px solid rgba(108,99,255,0.25);
    border-radius: 100px;
    padding: 6px 16px;
    font-size: 13px;
    font-weight: 500;
    color: var(--accent2);
    margin-bottom: 28px;
    letter-spacing: 0.3px;
    position: relative;
  }

  .eyebrow-dot {
    width: 6px; height: 6px;
    background: var(--accent2);
    border-radius: 50%;
    animation: blink 2s ease-in-out infinite;
  }

  @keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.3; }
  }

  .hero h1 {
    font-family: var(--font-display);
    font-size: clamp(40px, 6vw, 76px);
    font-weight: 800;
    line-height: 1.05;
    letter-spacing: -2px;
    color: var(--white);
    max-width: 900px;
    margin: 0 auto 24px;
    position: relative;
  }

  .hero h1 em {
    font-style: normal;
    color: var(--accent2);
  }

  .hero-sub {
    font-size: 18px;
    font-weight: 300;
    color: var(--muted);
    max-width: 560px;
    margin: 0 auto 40px;
    line-height: 1.7;
    position: relative;
  }

  .hero-actions {
    display: flex;
    align-items: center;
    gap: 16px;
    flex-wrap: wrap;
    justify-content: center;
    position: relative;
    margin-bottom: 64px;
  }

  .btn-primary {
    background: var(--accent);
    color: #fff;
    padding: 14px 32px;
    border-radius: 8px;
    font-family: var(--font-body);
    font-size: 15px;
    font-weight: 500;
    text-decoration: none;
    transition: background 0.2s, transform 0.2s;
    display: inline-flex;
    align-items: center;
    gap: 8px;
  }

  .btn-primary:hover { background: var(--accent2); transform: translateY(-2px); }

  .btn-ghost {
    background: transparent;
    color: var(--text);
    padding: 14px 32px;
    border-radius: 8px;
    border: 1px solid var(--border2);
    font-family: var(--font-body);
    font-size: 15px;
    font-weight: 400;
    text-decoration: none;
    transition: border-color 0.2s, transform 0.2s, background 0.2s;
    display: inline-flex;
    align-items: center;
    gap: 8px;
  }

  .btn-ghost:hover { border-color: var(--accent); background: var(--accent-dim2); transform: translateY(-2px); }

  /* Password highlight box */
  .password-highlight {
    display: inline-flex;
    align-items: center;
    gap: 12px;
    background: var(--accent-dim);
    border: 1px solid rgba(108,99,255,0.3);
    border-radius: 12px;
    padding: 12px 20px;
    position: relative;
    margin-bottom: 64px;
  }

  .pw-label {
    font-size: 13px;
    color: var(--muted);
    font-weight: 400;
  }

  .pw-badge {
    font-family: 'Courier New', monospace;
    font-size: 28px;
    font-weight: 700;
    color: var(--white);
    background: var(--accent);
    padding: 4px 16px;
    border-radius: 8px;
    letter-spacing: 4px;
    box-shadow: 0 0 20px rgba(108,99,255,0.4);
    animation: glow-pulse 3s ease-in-out infinite;
  }

  @keyframes glow-pulse {
    0%, 100% { box-shadow: 0 0 20px rgba(108,99,255,0.4); }
    50% { box-shadow: 0 0 40px rgba(108,99,255,0.7), 0 0 60px rgba(108,99,255,0.3); }
  }

  /* Stats row */
  .hero-stats {
    display: flex;
    gap: 48px;
    flex-wrap: wrap;
    justify-content: center;
    position: relative;
  }

  .stat-item {
    text-align: center;
  }

  .stat-num {
    font-family: var(--font-display);
    font-size: 36px;
    font-weight: 800;
    color: var(--white);
    line-height: 1;
    display: block;
  }

  .stat-label {
    font-size: 13px;
    color: var(--muted);
    font-weight: 400;
    display: block;
    margin-top: 4px;
    letter-spacing: 0.3px;
  }

  /* ── SECTIONS ── */
  section { padding: 100px 24px; position: relative; }

  .container { max-width: 1160px; margin: 0 auto; }

  .section-eyebrow {
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: var(--accent2);
    margin-bottom: 12px;
  }

  .section-title {
    font-family: var(--font-display);
    font-size: clamp(28px, 4vw, 48px);
    font-weight: 700;
    line-height: 1.1;
    letter-spacing: -1px;
    color: var(--white);
    margin-bottom: 16px;
  }

  .section-sub {
    font-size: 17px;
    font-weight: 300;
    color: var(--muted);
    max-width: 520px;
    line-height: 1.7;
  }

  /* ── TRUST BAR ── */
  .trust-bar {
    padding: 32px 24px;
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
    background: var(--bg2);
  }

  .trust-bar-inner {
    max-width: 1160px;
    margin: 0 auto;
    display: flex;
    align-items: center;
    gap: 32px;
    flex-wrap: wrap;
    justify-content: center;
  }

  .trust-label {
    font-size: 12px;
    font-weight: 500;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    color: var(--muted2);
    white-space: nowrap;
  }

  .trust-divider { width: 1px; height: 24px; background: var(--border2); }

  .brand-logos {
    display: flex;
    gap: 40px;
    align-items: center;
    flex-wrap: wrap;
    justify-content: center;
  }

  .brand-pill {
    font-family: var(--font-display);
    font-size: 15px;
    font-weight: 700;
    color: var(--muted2);
    letter-spacing: 0.5px;
    transition: color 0.2s;
    cursor: default;
  }

  .brand-pill:hover { color: var(--muted); }

  /* ── METRICS ── */
  .metrics-grid {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 1px;
    background: var(--border);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    overflow: hidden;
    margin-top: 56px;
  }

  .metric-cell {
    background: var(--card);
    padding: 40px 28px;
    text-align: center;
    transition: background 0.2s;
  }

  .metric-cell:hover { background: var(--bg3); }

  .metric-big {
    font-family: var(--font-display);
    font-size: 48px;
    font-weight: 800;
    color: var(--white);
    line-height: 1;
    display: block;
    margin-bottom: 8px;
  }

  .metric-big span { color: var(--accent2); }

  .metric-desc {
    font-size: 13px;
    color: var(--muted);
    line-height: 1.5;
  }

  /* ── PROBLEM/SOLUTION ── */
  .ps-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 32px;
    margin-top: 56px;
  }

  .ps-card {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 36px 32px;
  }

  .ps-card.solution { border-color: rgba(108,99,255,0.3); background: var(--accent-dim2); }

  .ps-card-label {
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 2px;
    text-transform: uppercase;
    margin-bottom: 20px;
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .ps-card-label.problem { color: #f87171; }
  .ps-card-label.solution { color: var(--accent2); }

  .ps-card-label::before {
    content: '';
    display: inline-block;
    width: 6px; height: 6px;
    border-radius: 50%;
    background: currentColor;
  }

  .problems-list {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .problems-list li {
    display: flex;
    align-items: center;
    gap: 12px;
    font-size: 15px;
    color: var(--muted);
    padding: 12px 16px;
    background: rgba(248,113,113,0.05);
    border: 1px solid rgba(248,113,113,0.1);
    border-radius: 8px;
  }

  .problems-list li::before {
    content: '×';
    color: #f87171;
    font-size: 18px;
    font-weight: 700;
    flex-shrink: 0;
    width: 20px;
    text-align: center;
  }

  .solution-text {
    font-size: 17px;
    font-weight: 300;
    color: var(--text);
    line-height: 1.8;
    margin-bottom: 24px;
  }

  /* ── PORTFOLIO ── */
  .portfolio-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
    margin-top: 56px;
  }

  .portfolio-card {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    overflow: hidden;
    text-decoration: none;
    color: inherit;
    transition: transform 0.3s cubic-bezier(0.16,1,0.3,1), border-color 0.3s, box-shadow 0.3s;
    display: flex;
    flex-direction: column;
  }

  .portfolio-card:hover {
    transform: translateY(-8px);
    border-color: rgba(108,99,255,0.3);
    box-shadow: 0 24px 48px rgba(0,0,0,0.4), 0 0 0 1px rgba(108,99,255,0.15);
  }

  .portfolio-browser {
    height: 36px;
    background: #1a1a1a;
    border-bottom: 1px solid var(--border);
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 0 14px;
    flex-shrink: 0;
  }

  .pdot { width: 9px; height: 9px; border-radius: 50%; }
  .pdot.r { background: #ef4444; opacity: 0.7; }
  .pdot.y { background: #f59e0b; opacity: 0.7; }
  .pdot.g { background: #22c55e; opacity: 0.7; }

  .portfolio-img {
    aspect-ratio: 16/10;
    overflow: hidden;
    position: relative;
    background: var(--bg3);
  }

  .portfolio-img img {
    width: 100%; height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 0.6s cubic-bezier(0.16,1,0.3,1);
  }

  .portfolio-card:hover .portfolio-img img { transform: scale(1.06); }

  .portfolio-overlay {
    position: absolute;
    inset: 0;
    background: rgba(10,10,10,0.7);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    opacity: 0;
    transition: opacity 0.3s;
    gap: 10px;
  }

  .portfolio-card:hover .portfolio-overlay { opacity: 1; }

  .overlay-title {
    font-family: var(--font-display);
    font-size: 20px;
    font-weight: 700;
    color: var(--white);
    transform: translateY(8px);
    transition: transform 0.3s cubic-bezier(0.16,1,0.3,1);
  }

  .portfolio-card:hover .overlay-title { transform: translateY(0); }

  .overlay-btn {
    background: rgba(108,99,255,0.85);
    color: #fff;
    font-size: 13px;
    font-weight: 500;
    padding: 8px 18px;
    border-radius: 100px;
    transform: translateY(8px);
    transition: transform 0.3s cubic-bezier(0.16,1,0.3,1) 0.05s;
  }

  .portfolio-card:hover .overlay-btn { transform: translateY(0); }

  .portfolio-meta {
    padding: 16px 18px;
    background: var(--card);
    border-top: 1px solid var(--border);
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .p-meta-title {
    font-size: 14px;
    font-weight: 500;
    color: var(--white);
  }

  .p-meta-tag {
    font-size: 11px;
    font-weight: 500;
    background: var(--accent-dim);
    color: var(--accent2);
    padding: 3px 9px;
    border-radius: 4px;
    letter-spacing: 0.3px;
  }

  /* ── SERVICES ── */
  .services-section { background: var(--bg2); }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1px;
    background: var(--border);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    overflow: hidden;
    margin-top: 56px;
  }

  .service-card {
    background: var(--card);
    padding: 36px 28px;
    transition: background 0.2s;
  }

  .service-card:hover { background: #181818; }

  .service-icon {
    width: 44px; height: 44px;
    background: var(--accent-dim);
    border: 1px solid rgba(108,99,255,0.2);
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 20px;
    font-size: 20px;
  }

  .service-title {
    font-family: var(--font-display);
    font-size: 17px;
    font-weight: 700;
    color: var(--white);
    margin-bottom: 8px;
  }

  .service-desc { font-size: 14px; color: var(--muted); line-height: 1.65; }

  /* ── COMPARISON ── */
  .comparison-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 24px;
    margin-top: 56px;
  }

  .comp-card {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    overflow: hidden;
  }

  .comp-card.us { border-color: rgba(108,99,255,0.35); }

  .comp-header {
    padding: 20px 24px;
    border-bottom: 1px solid var(--border);
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .comp-header.us { background: var(--accent-dim); border-bottom-color: rgba(108,99,255,0.2); }

  .comp-header-title {
    font-family: var(--font-display);
    font-size: 15px;
    font-weight: 700;
    color: var(--white);
  }

  .comp-badge {
    font-size: 11px;
    font-weight: 600;
    background: var(--accent);
    color: #fff;
    padding: 2px 8px;
    border-radius: 4px;
    letter-spacing: 0.3px;
    margin-left: auto;
  }

  .comp-list { list-style: none; padding: 20px 24px; display: flex; flex-direction: column; gap: 10px; }

  .comp-list li {
    font-size: 14px;
    color: var(--muted);
    padding-left: 20px;
    position: relative;
    line-height: 1.5;
  }

  .comp-list li::before {
    content: '';
    position: absolute;
    left: 0;
    top: 8px;
    width: 6px; height: 6px;
    border-radius: 50%;
    background: var(--muted2);
  }

  .comp-list.us li { color: var(--text); }
  .comp-list.us li::before { background: var(--accent2); }

  /* ── TEAM ── */
  .team-grid {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 20px;
    margin-top: 56px;
  }

  .team-card {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 28px 20px 24px;
    text-align: center;
    transition: transform 0.3s, border-color 0.3s;
  }

  .team-card:hover { transform: translateY(-4px); border-color: rgba(108,99,255,0.25); }

  .team-avatar {
    width: 64px; height: 64px;
    border-radius: 50%;
    background: var(--accent-dim);
    border: 2px solid rgba(108,99,255,0.25);
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 16px;
    font-family: var(--font-display);
    font-size: 22px;
    font-weight: 800;
    color: var(--accent2);
  }

  .team-name {
    font-family: var(--font-display);
    font-size: 16px;
    font-weight: 700;
    color: var(--white);
    margin-bottom: 4px;
  }

  .team-role {
    font-size: 12px;
    color: var(--accent2);
    font-weight: 500;
    margin-bottom: 10px;
    letter-spacing: 0.2px;
  }

  .team-desc { font-size: 12px; color: var(--muted); line-height: 1.55; }

  /* ── PROCESS ── */
  .process-section { background: var(--bg2); }

  .process-steps {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 0;
    margin-top: 56px;
    position: relative;
  }

  .process-step {
    padding: 36px 24px;
    position: relative;
    background: var(--card);
    border: 1px solid var(--border);
    margin-right: -1px;
    margin-bottom: -1px;
    transition: background 0.2s;
  }

  .process-step:hover { background: #181818; }

  .step-number {
    font-family: var(--font-display);
    font-size: 48px;
    font-weight: 800;
    color: rgba(108,99,255,0.15);
    line-height: 1;
    margin-bottom: 12px;
    display: block;
  }

  .step-title {
    font-family: var(--font-display);
    font-size: 16px;
    font-weight: 700;
    color: var(--white);
    margin-bottom: 8px;
  }

  .step-desc { font-size: 13px; color: var(--muted); line-height: 1.6; }

  /* ── TESTIMONIALS ── */
  .testimonials-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
    margin-top: 56px;
  }

  .testimonial-card {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 32px;
    transition: border-color 0.2s;
  }

  .testimonial-card:hover { border-color: rgba(108,99,255,0.25); }

  .stars {
    display: flex;
    gap: 4px;
    margin-bottom: 20px;
  }

  .star {
    width: 14px; height: 14px;
    background: #f59e0b;
    clip-path: polygon(50% 0%, 61% 35%, 98% 35%, 68% 57%, 79% 91%, 50% 70%, 21% 91%, 32% 57%, 2% 35%, 39% 35%);
  }

  .testimonial-text {
    font-size: 15px;
    font-style: italic;
    font-weight: 300;
    color: var(--text);
    line-height: 1.75;
    margin-bottom: 24px;
  }

  .testimonial-text::before { content: '"'; }
  .testimonial-text::after { content: '"'; }

  .testimonial-author {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .author-avatar {
    width: 40px; height: 40px;
    border-radius: 50%;
    background: var(--accent-dim);
    border: 1px solid rgba(108,99,255,0.2);
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: var(--font-display);
    font-size: 14px;
    font-weight: 700;
    color: var(--accent2);
    flex-shrink: 0;
  }

  .author-name { font-size: 14px; font-weight: 500; color: var(--white); }
  .author-loc { font-size: 12px; color: var(--muted); }

  /* ── SKILLS GRID ── */
  .skills-grid {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    margin-top: 40px;
  }

  .skill-pill {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: 6px;
    padding: 8px 16px;
    font-size: 13px;
    font-weight: 500;
    color: var(--muted);
    transition: color 0.2s, border-color 0.2s, background 0.2s;
    cursor: default;
  }

  .skill-pill:hover { color: var(--text); border-color: rgba(108,99,255,0.3); background: var(--accent-dim2); }

  /* ── DELIVERABLES ── */
  .deliverables-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 16px;
    margin-top: 56px;
  }

  .deliv-item {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
    padding: 20px;
    display: flex;
    align-items: flex-start;
    gap: 12px;
    transition: border-color 0.2s;
  }

  .deliv-item:hover { border-color: rgba(108,99,255,0.25); }

  .deliv-check {
    width: 20px; height: 20px;
    border-radius: 50%;
    background: rgba(34,197,94,0.12);
    border: 1px solid rgba(34,197,94,0.3);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    margin-top: 1px;
  }

  .deliv-check::before {
    content: '✓';
    font-size: 10px;
    color: #4ade80;
    font-weight: 700;
  }

  .deliv-text { font-size: 14px; color: var(--muted); line-height: 1.5; }

  /* ── FAQ ── */
  .faq-list {
    margin-top: 56px;
    display: flex;
    flex-direction: column;
    gap: 1px;
    background: var(--border);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    overflow: hidden;
  }

  .faq-item {
    background: var(--card);
    padding: 24px 28px;
    cursor: pointer;
    transition: background 0.2s;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
  }

  .faq-item:hover { background: #181818; }

  .faq-question { font-size: 15px; font-weight: 500; color: var(--text); }

  .faq-arrow {
    color: var(--muted2);
    font-size: 18px;
    transition: transform 0.2s, color 0.2s;
    flex-shrink: 0;
  }

  .faq-item:hover .faq-arrow { color: var(--accent2); transform: translateX(4px); }

  /* ── TRANSPARENCY ── */
  .transparency-section { background: var(--bg2); }

  .transparency-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 16px;
    margin-top: 40px;
  }

  .transparency-item {
    background: var(--card);
    border: 1px solid var(--border);
    border-left: 3px solid rgba(108,99,255,0.5);
    border-radius: 8px;
    padding: 20px 20px 20px 24px;
    font-size: 15px;
    color: var(--muted);
    line-height: 1.55;
    font-weight: 300;
  }

  /* ── AUDIT SECTION ── */
  .audit-section {
    padding: 80px 24px;
    background: var(--bg2);
  }

  .audit-inner {
    max-width: 800px;
    margin: 0 auto;
    text-align: center;
    background: var(--card);
    border: 1px solid rgba(108,99,255,0.25);
    border-radius: 20px;
    padding: 64px 48px;
    position: relative;
    overflow: hidden;
  }

  .audit-inner::before {
    content: '';
    position: absolute;
    top: -80px; left: 50%; transform: translateX(-50%);
    width: 300px; height: 300px;
    background: radial-gradient(circle, rgba(108,99,255,0.15) 0%, transparent 70%);
    pointer-events: none;
  }

  .audit-title {
    font-family: var(--font-display);
    font-size: 36px;
    font-weight: 800;
    color: var(--white);
    letter-spacing: -1px;
    margin-bottom: 12px;
    position: relative;
  }

  .audit-desc {
    font-size: 16px;
    color: var(--muted);
    max-width: 480px;
    margin: 0 auto 32px;
    line-height: 1.7;
    font-weight: 300;
    position: relative;
  }

  /* ── FINAL CTA ── */
  .final-cta {
    padding: 120px 24px;
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  .final-cta::before {
    content: '';
    position: absolute;
    top: 50%; left: 50%; transform: translate(-50%, -50%);
    width: 700px; height: 700px;
    background: radial-gradient(circle, rgba(108,99,255,0.08) 0%, transparent 70%);
    pointer-events: none;
  }

  .final-cta-title {
    font-family: var(--font-display);
    font-size: clamp(36px, 5vw, 64px);
    font-weight: 800;
    color: var(--white);
    letter-spacing: -2px;
    margin-bottom: 16px;
    position: relative;
    line-height: 1.05;
  }

  .final-cta-sub {
    font-size: 18px;
    color: var(--muted);
    max-width: 460px;
    margin: 0 auto 40px;
    font-weight: 300;
    position: relative;
    line-height: 1.7;
  }

  /* ── FOOTER ── */
  footer {
    background: var(--bg2);
    border-top: 1px solid var(--border);
    padding: 56px 24px 40px;
  }

  .footer-inner {
    max-width: 1160px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1.5fr 1fr 1fr 1fr;
    gap: 48px;
  }

  .footer-brand-name {
    font-family: var(--font-display);
    font-size: 20px;
    font-weight: 800;
    color: var(--white);
    margin-bottom: 12px;
  }

  .footer-brand-name span { color: var(--accent2); }

  .footer-tagline { font-size: 14px; color: var(--muted); line-height: 1.6; max-width: 240px; font-weight: 300; }

  .footer-col-title {
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 1.5px;
    text-transform: uppercase;
    color: var(--muted2);
    margin-bottom: 20px;
  }

  .footer-links { list-style: none; display: flex; flex-direction: column; gap: 12px; }

  .footer-links a {
    font-size: 14px;
    color: var(--muted);
    text-decoration: none;
    transition: color 0.2s;
  }

  .footer-links a:hover { color: var(--text); }

  .footer-bottom {
    max-width: 1160px;
    margin: 48px auto 0;
    padding-top: 24px;
    border-top: 1px solid var(--border);
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 16px;
    flex-wrap: wrap;
  }

  .footer-copy { font-size: 13px; color: var(--muted2); }

  .footer-socials {
    display: flex;
    gap: 20px;
    list-style: none;
  }

  .footer-socials a {
    font-size: 13px;
    color: var(--muted2);
    text-decoration: none;
    transition: color 0.2s;
  }

  .footer-socials a:hover { color: var(--muted); }

  /* ── INDUSTRIES ── */
  .industries-pills {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    margin-top: 40px;
  }

  .industry-pill {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: 100px;
    padding: 10px 22px;
    font-size: 14px;
    color: var(--muted);
    font-weight: 400;
    transition: all 0.2s;
    cursor: default;
  }

  .industry-pill:hover {
    color: var(--accent2);
    border-color: rgba(108,99,255,0.3);
    background: var(--accent-dim2);
  }

  /* ── FUTURE VISION ── */
  .vision-card {
    background: linear-gradient(135deg, var(--card) 0%, #1a1630 100%);
    border: 1px solid rgba(108,99,255,0.25);
    border-radius: var(--radius);
    padding: 48px;
    margin-top: 56px;
    display: grid;
    grid-template-columns: 1fr auto;
    align-items: center;
    gap: 32px;
  }

  .vision-title {
    font-family: var(--font-display);
    font-size: 24px;
    font-weight: 700;
    color: var(--white);
    margin-bottom: 12px;
  }

  .vision-text {
    font-size: 16px;
    color: var(--muted);
    line-height: 1.7;
    font-weight: 300;
    max-width: 560px;
  }

  .vision-badge {
    background: var(--accent-dim);
    border: 1px solid rgba(108,99,255,0.3);
    border-radius: 12px;
    padding: 20px 28px;
    text-align: center;
    white-space: nowrap;
    flex-shrink: 0;
  }

  .vision-badge-title {
    font-family: var(--font-display);
    font-size: 13px;
    font-weight: 700;
    color: var(--accent2);
    letter-spacing: 1.5px;
    text-transform: uppercase;
    margin-bottom: 4px;
  }

  .vision-badge-text { font-size: 14px; color: var(--muted); }

  /* ── ANIMATIONS ── */
  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(24px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .fade-up { animation: fadeUp 0.6s ease-out both; }
  .delay-1 { animation-delay: 0.1s; }
  .delay-2 { animation-delay: 0.2s; }
  .delay-3 { animation-delay: 0.3s; }
  .delay-4 { animation-delay: 0.4s; }

  /* ── RESPONSIVE ── */
  @media (max-width: 1024px) {
    nav { padding: 0 28px; }
    .nav-links { gap: 24px; }
    .metrics-grid { grid-template-columns: repeat(3, 1fr); }
    .team-grid { grid-template-columns: repeat(3, 1fr); }
    .footer-inner { grid-template-columns: 1fr 1fr; gap: 36px; }
    .process-steps { grid-template-columns: repeat(2, 1fr); }
  }

  @media (max-width: 768px) {
    nav { padding: 0 20px; }
    .nav-links { display: none; }
    .portfolio-grid { grid-template-columns: 1fr; }
    .services-grid { grid-template-columns: 1fr 1fr; }
    .ps-grid { grid-template-columns: 1fr; }
    .comparison-grid { grid-template-columns: 1fr; }
    .team-grid { grid-template-columns: repeat(2, 1fr); }
    .deliverables-grid { grid-template-columns: repeat(2, 1fr); }
    .testimonials-grid { grid-template-columns: 1fr; }
    .transparency-grid { grid-template-columns: 1fr; }
    .vision-card { grid-template-columns: 1fr; }
    .footer-inner { grid-template-columns: 1fr; }
    .hero-stats { gap: 32px; }
    section { padding: 72px 24px; }
    .metrics-grid { grid-template-columns: repeat(2, 1fr); }
    .process-steps { grid-template-columns: 1fr; }
  }

  @media (max-width: 480px) {
    .services-grid { grid-template-columns: 1fr; }
    .deliverables-grid { grid-template-columns: 1fr; }
    .hero h1 { letter-spacing: -1px; }
  }
</style>
</head>
<body>

<!-- NAV -->
<nav>
  <a href="#" class="nav-logo">MAKE<span>DIGITAL</span></a>
  <ul class="nav-links">
    <li><a href="#work">Work</a></li>
    <li><a href="#services">Services</a></li>
    <li><a href="#process">Process</a></li>
    <li><a href="#team">Team</a></li>
    <li><a href="#reviews">Reviews</a></li>
    <li><a href="#contact" class="nav-cta">Book a Call</a></li>
  </ul>
</nav>

<!-- HERO -->
<section class="hero">
  <div class="hero-bg">
    <div class="hero-orb orb1"></div>
    <div class="hero-orb orb2"></div>
    <div class="hero-orb orb3"></div>
    <div class="hero-grid-overlay"></div>
  </div>

  <div class="hero-eyebrow fade-up">
    <span class="eyebrow-dot"></span>
    Premium Shopify Agency · India
  </div>

  <h1 class="fade-up delay-1">
    We Build Shopify Stores<br>That <em>Look Premium</em><br>And Convert Better
  </h1>

  <p class="hero-sub fade-up delay-2">
    200+ Shopify Stores Designed. 50+ Stores Managed. Built for brands that want more than just a pretty website.
  </p>

  <div class="hero-actions fade-up delay-3">
    <a href="#work" class="btn-primary">View Portfolio →</a>
    <a href="#contact" class="btn-ghost">Book Discovery Call</a>
  </div>

  <div class="password-highlight fade-up delay-3">
    <span class="pw-label">Store Preview Access Password</span>
    <span class="pw-badge">321</span>
  </div>

  <div class="hero-stats fade-up delay-4">
    <div class="stat-item">
      <span class="stat-num">200+</span>
      <span class="stat-label">Stores Designed</span>
    </div>
    <div class="stat-item">
      <span class="stat-num">50+</span>
      <span class="stat-label">Stores Managed</span>
    </div>
    <div class="stat-item">
      <span class="stat-num">2+</span>
      <span class="stat-label">Years Experience</span>
    </div>
    <div class="stat-item">
      <span class="stat-num">5</span>
      <span class="stat-label">Specialists</span>
    </div>
  </div>
</section>

<!-- TRUST BAR -->
<div class="trust-bar">
  <div class="trust-bar-inner">
    <span class="trust-label">Trusted By</span>
    <div class="trust-divider"></div>
    <div class="brand-logos">
      <span class="brand-pill">AuraJewels</span>
      <span class="brand-pill">AtelierRTW</span>
      <span class="brand-pill">VedaBotanicals</span>
      <span class="brand-pill">LuminaHome</span>
      <span class="brand-pill">InkBrands</span>
      <span class="brand-pill">NoirFashion</span>
    </div>
  </div>
</div>

<!-- METRICS -->
<section>
  <div class="container">
    <p class="section-eyebrow">Numbers Backed By Work</p>
    <h2 class="section-title">Results We've Delivered</h2>
    <p class="section-sub">Every number on this page represents real stores, real clients, and real outcomes.</p>
    <div class="metrics-grid">
      <div class="metric-cell">
        <span class="metric-big">200<span>+</span></span>
        <span class="metric-desc">Shopify projects<br>completed</span>
      </div>
      <div class="metric-cell">
        <span class="metric-big">50<span>+</span></span>
        <span class="metric-desc">Active store<br>management clients</span>
      </div>
      <div class="metric-cell">
        <span class="metric-big">2<span>+</span></span>
        <span class="metric-desc">Years of focused<br>Shopify experience</span>
      </div>
      <div class="metric-cell">
        <span class="metric-big">8<span>+</span></span>
        <span class="metric-desc">Industry niches<br>worked in</span>
      </div>
      <div class="metric-cell">
        <span class="metric-big">5</span>
        <span class="metric-desc">Dedicated in-house<br>specialists</span>
      </div>
    </div>
  </div>
</section>

<!-- PROBLEM / SOLUTION -->
<section>
  <div class="container">
    <p class="section-eyebrow">Why Stores Fail</p>
    <h2 class="section-title">Most Shopify Stores<br>Leave Money on the Table</h2>
    <div class="ps-grid">
      <div class="ps-card">
        <div class="ps-card-label problem">Common Problems</div>
        <ul class="problems-list">
          <li>Generic template look and feel</li>
          <li>Poor buying confidence and trust signals</li>
          <li>Weak product presentation</li>
          <li>Slow loading pages</li>
          <li>Poor mobile experience</li>
          <li>No conversion strategy</li>
        </ul>
      </div>
      <div class="ps-card solution">
        <div class="ps-card-label solution">Our Solution</div>
        <p class="solution-text">Our team focuses on design, development, and customer journey optimization — creating stores that feel trustworthy, professional, and drive real sales. Every decision we make is rooted in conversion psychology.</p>
        <a href="#contact" class="btn-primary" style="display: inline-flex; font-size: 14px; padding: 11px 24px;">Let's fix your store →</a>
      </div>
    </div>
  </div>
</section>

<!-- PORTFOLIO -->
<section id="work">
  <div class="container">
    <p class="section-eyebrow">Portfolio</p>
    <h2 class="section-title">Selected Shopify Projects</h2>
    <p class="section-sub">Real stores. Real work. Hover to preview.</p>
    <div class="portfolio-grid">

      <a href="https://make-digital-jewellery.myshopify.com" target="_blank" class="portfolio-card">
        <div class="portfolio-browser">
          <span class="pdot r"></span>
          <span class="pdot y"></span>
          <span class="pdot g"></span>
        </div>
        <div class="portfolio-img">
          <img src="https://cdn.prod.website-files.com/64da807a9aa000087e97b92d/64edcff5fd271e49b7b760ca_63ea592e693af84f9466f5bd_sofiathumbnail.jpeg" alt="Luxury Jewellery Shopify Store" loading="lazy">
          <div class="portfolio-overlay">
            <div class="overlay-title">Jewellery Store</div>
            <div class="overlay-btn">Launch Preview →</div>
          </div>
        </div>
        <div class="portfolio-meta">
          <span class="p-meta-title">Aura Luxury Fine Jewels</span>
          <span class="p-meta-tag">Shopify Plus</span>
        </div>
      </a>

      <a href="https://make-digital-jewellery.myshopify.com" target="_blank" class="portfolio-card">
        <div class="portfolio-browser">
          <span class="pdot r"></span>
          <span class="pdot y"></span>
          <span class="pdot g"></span>
        </div>
        <div class="portfolio-img">
          <img src="https://cdn.dribbble.com/userupload/45294572/file/a9e7881ee47039b0750032d449252d73.png?resize=1504x1074&vertical=center" alt="Fashion Ready-to-Wear Shopify Store" loading="lazy">
          <div class="portfolio-overlay">
            <div class="overlay-title">Fashion Store</div>
            <div class="overlay-btn">Launch Preview →</div>
          </div>
        </div>
        <div class="portfolio-meta">
          <span class="p-meta-title">Atelier Ready-To-Wear</span>
          <span class="p-meta-tag">E-Commerce</span>
        </div>
      </a>

      <a href="https://make-digital-jewellery.myshopify.com" target="_blank" class="portfolio-card">
        <div class="portfolio-browser">
          <span class="pdot r"></span>
          <span class="pdot y"></span>
          <span class="pdot g"></span>
        </div>
        <div class="portfolio-img">
          <img src="https://cdn.dribbble.com/userupload/18238426/file/original-2070877c6941bd0341672e31fe28c1c5.png?resize=1504x826&vertical=center" alt="Ayurvedic Wellness Shopify Store" loading="lazy">
          <div class="portfolio-overlay">
            <div class="overlay-title">Wellness Store</div>
            <div class="overlay-btn">Launch Preview →</div>
          </div>
        </div>
        <div class="portfolio-meta">
          <span class="p-meta-title">Veda Botanicals Wellness</span>
          <span class="p-meta-tag">Organic CMS</span>
        </div>
      </a>

    </div>
  </div>
</section>

<!-- SERVICES -->
<section id="services" class="services-section">
  <div class="container">
    <p class="section-eyebrow">Services</p>
    <h2 class="section-title">What We Build For You</h2>
    <p class="section-sub">End-to-end Shopify expertise under one roof — from first pixel to ongoing operations.</p>
    <div class="services-grid">

      <div class="service-card">
        <div class="service-icon">🎨</div>
        <div class="service-title">Shopify Store Design</div>
        <p class="service-desc">Premium storefront design focused on trust-building, brand identity, and conversion-optimized layouts.</p>
      </div>

      <div class="service-card">
        <div class="service-icon">⚙️</div>
        <div class="service-title">Custom Development</div>
        <p class="service-desc">Advanced theme customization, custom sections, Liquid development, and bespoke functionality.</p>
      </div>

      <div class="service-card">
        <div class="service-icon">📦</div>
        <div class="service-title">Store Management</div>
        <p class="service-desc">Product uploads, content updates, app management, and seamless daily store operations.</p>
      </div>

      <div class="service-card">
        <div class="service-icon">📈</div>
        <div class="service-title">Conversion Optimization</div>
        <p class="service-desc">Improving customer journeys, reducing friction, and systematically increasing your sales rate.</p>
      </div>

      <div class="service-card">
        <div class="service-icon">⚡</div>
        <div class="service-title">Speed Optimization</div>
        <p class="service-desc">Faster stores with improved Lighthouse scores, Core Web Vitals, and real-world performance gains.</p>
      </div>

      <div class="service-card">
        <div class="service-icon">🔄</div>
        <div class="service-title">Store Migration</div>
        <p class="service-desc">Move your store to Shopify without losing products, data, SEO rankings, or customer history.</p>
      </div>

    </div>
  </div>
</section>

<!-- INDUSTRIES -->
<section>
  <div class="container">
    <p class="section-eyebrow">Industries</p>
    <h2 class="section-title">Niches We've Worked In</h2>
    <p class="section-sub">Deep experience across the brands and product categories that matter most on Shopify.</p>
    <div class="industries-pills">
      <span class="industry-pill">💍 Jewellery</span>
      <span class="industry-pill">👗 Fashion</span>
      <span class="industry-pill">💄 Beauty</span>
      <span class="industry-pill">👜 Accessories</span>
      <span class="industry-pill">🌿 Lifestyle</span>
      <span class="industry-pill">🔌 Electronics</span>
      <span class="industry-pill">🏠 Home Decor</span>
      <span class="industry-pill">📦 D2C Brands</span>
    </div>
  </div>
</section>

<!-- COMPARISON -->
<section>
  <div class="container">
    <p class="section-eyebrow">Why Choose Us</p>
    <h2 class="section-title">A Dedicated Team<br>Beats a Solo Freelancer</h2>
    <p class="section-sub">When you hire us, you get five specialists working in sync — not one person wearing all the hats.</p>
    <div class="comparison-grid">
      <div class="comp-card">
        <div class="comp-header">
          <span class="comp-header-title">Solo Freelancer</span>
        </div>
        <ul class="comp-list">
          <li>One person handles everything</li>
          <li>Limited availability and bandwidth</li>
          <li>Single skill focus, generalist gaps</li>
          <li>Bottleneck on every revision</li>
          <li>Inconsistent delivery timelines</li>
        </ul>
      </div>
      <div class="comp-card us">
        <div class="comp-header us">
          <span class="comp-header-title">Shopify Growth Studio</span>
          <span class="comp-badge">Recommended</span>
        </div>
        <ul class="comp-list us">
          <li>Dedicated UI/UX Designer</li>
          <li>Dedicated Frontend Developer</li>
          <li>Dedicated Store Handler</li>
          <li>Dedicated Client Onboarding</li>
          <li>Dedicated Graphic Designer</li>
        </ul>
      </div>
    </div>
  </div>
</section>

<!-- EXPERTISE -->
<section style="background: var(--bg2);">
  <div class="container">
    <p class="section-eyebrow">Expertise</p>
    <h2 class="section-title">What We Work On Daily</h2>
    <p class="section-sub">Our team lives and breathes the Shopify ecosystem — these are the tools we touch every day.</p>
    <div class="skills-grid">
      <span class="skill-pill">Liquid</span>
      <span class="skill-pill">HTML</span>
      <span class="skill-pill">CSS</span>
      <span class="skill-pill">JavaScript</span>
      <span class="skill-pill">Metafields</span>
      <span class="skill-pill">Theme Development</span>
      <span class="skill-pill">Landing Pages</span>
      <span class="skill-pill">Collection Pages</span>
      <span class="skill-pill">Product Pages</span>
      <span class="skill-pill">Shopify Apps</span>
      <span class="skill-pill">Performance Optimization</span>
      <span class="skill-pill">Store Management</span>
      <span class="skill-pill">Figma</span>
      <span class="skill-pill">Meta Pixel</span>
      <span class="skill-pill">Google Analytics</span>
    </div>
  </div>
</section>

<!-- TEAM -->
<section id="team">
  <div class="container">
    <p class="section-eyebrow">The Team</p>
    <h2 class="section-title">Meet the People<br>Behind Your Store</h2>
    <p class="section-sub">Five specialists, one shared obsession: making your Shopify store perform.</p>
    <div class="team-grid">

      <div class="team-card">
        <div class="team-avatar">P</div>
        <div class="team-name">Pratham</div>
        <div class="team-role">UI/UX Designer</div>
        <p class="team-desc">Creates premium customer experiences and intuitive storefront interfaces.</p>
      </div>

      <div class="team-card">
        <div class="team-avatar">S</div>
        <div class="team-name">Suyash</div>
        <div class="team-role">Frontend Developer</div>
        <p class="team-desc">Builds responsive and interactive storefront experiences in Liquid and JS.</p>
      </div>

      <div class="team-card">
        <div class="team-avatar">D</div>
        <div class="team-name">Deep</div>
        <div class="team-role">Store Handling & Dev</div>
        <p class="team-desc">Handles Shopify customization, development, and day-to-day store operations.</p>
      </div>

      <div class="team-card">
        <div class="team-avatar">R</div>
        <div class="team-name">Rachit</div>
        <div class="team-role">Client Onboarding</div>
        <p class="team-desc">Manages onboarding, client communication, and project coordination.</p>
      </div>

      <div class="team-card">
        <div class="team-avatar">SK</div>
        <div class="team-name">Saksham</div>
        <div class="team-role">Graphic Designer</div>
        <p class="team-desc">Creates branding assets, banners, and marketing visuals for stores.</p>
      </div>

    </div>
  </div>
</section>

<!-- PROCESS -->
<section id="process" class="process-section">
  <div class="container">
    <p class="section-eyebrow">Our Process</p>
    <h2 class="section-title">How We Work</h2>
    <p class="section-sub">A clear, structured process that keeps your project on track from kickoff to launch.</p>
    <div class="process-steps">

      <div class="process-step">
        <span class="step-number">01</span>
        <div class="step-title">Discovery Call</div>
        <p class="step-desc">We understand your brand, goals, target audience, and what success looks like for you.</p>
      </div>

      <div class="process-step">
        <span class="step-number">02</span>
        <div class="step-title">Planning & Research</div>
        <p class="step-desc">Competitor analysis, customer journey mapping, and a clear project roadmap.</p>
      </div>

      <div class="process-step">
        <span class="step-number">03</span>
        <div class="step-title">UI/UX Design</div>
        <p class="step-desc">High-fidelity Figma designs reviewed and approved before a single line of code is written.</p>
      </div>

      <div class="process-step">
        <span class="step-number">04</span>
        <div class="step-title">Development</div>
        <p class="step-desc">Precise Liquid and frontend development translating designs to a fully functional store.</p>
      </div>

      <div class="process-step">
        <span class="step-number">05</span>
        <div class="step-title">Testing</div>
        <p class="step-desc">Cross-device QA, speed checks, checkout flows, and accessibility review.</p>
      </div>

      <div class="process-step">
        <span class="step-number">06</span>
        <div class="step-title">Launch</div>
        <p class="step-desc">Smooth go-live with no downtime. Your new store is live and ready to sell.</p>
      </div>

      <div class="process-step">
        <span class="step-number">07</span>
        <div class="step-title">Post-Launch Support</div>
        <p class="step-desc">We don't disappear. Ongoing support, tweaks, and monitoring after launch.</p>
      </div>

      <div class="process-step">
        <span class="step-number">08</span>
        <div class="step-title">Ongoing Partnership</div>
        <p class="step-desc">Store management, growth collaboration, and continued optimization as your brand scales.</p>
      </div>

    </div>
  </div>
</section>

<!-- DELIVERABLES -->
<section>
  <div class="container">
    <p class="section-eyebrow">Every Store Includes</p>
    <h2 class="section-title">What's Built Into Every Project</h2>
    <p class="section-sub">No nickel-and-diming. These are standard across every store we deliver.</p>
    <div class="deliverables-grid">
      <div class="deliv-item"><div class="deliv-check"></div><span class="deliv-text">Mobile Responsive Design</span></div>
      <div class="deliv-item"><div class="deliv-check"></div><span class="deliv-text">Speed Optimization</span></div>
      <div class="deliv-item"><div class="deliv-check"></div><span class="deliv-text">Trust Elements & Social Proof</span></div>
      <div class="deliv-item"><div class="deliv-check"></div><span class="deliv-text">SEO Friendly Structure</span></div>
      <div class="deliv-item"><div class="deliv-check"></div><span class="deliv-text">Conversion Focused Layout</span></div>
      <div class="deliv-item"><div class="deliv-check"></div><span class="deliv-text">Cross Device Testing</span></div>
      <div class="deliv-item"><div class="deliv-check"></div><span class="deliv-text">Clean Navigation</span></div>
      <div class="deliv-item"><div class="deliv-check"></div><span class="deliv-text">Scalable Store Setup</span></div>
    </div>
  </div>
</section>

<!-- TESTIMONIALS -->
<section id="reviews" style="background: var(--bg2);">
  <div class="container">
    <p class="section-eyebrow">Client Reviews</p>
    <h2 class="section-title">What Our Clients Say</h2>
    <p class="section-sub">Real feedback from real founders who trusted us with their stores.</p>
    <div class="testimonials-grid">

      <div class="testimonial-card">
        <div class="stars"><div class="star"></div><div class="star"></div><div class="star"></div><div class="star"></div><div class="star"></div></div>
        <p class="testimonial-text">Professional communication, strong execution and a much more premium customer experience after launch.</p>
        <div class="testimonial-author">
          <div class="author-avatar">RS</div>
          <div>
            <div class="author-name">Rahul Sharma</div>
            <div class="author-loc">Mumbai</div>
          </div>
        </div>
      </div>

      <div class="testimonial-card">
        <div class="stars"><div class="star"></div><div class="star"></div><div class="star"></div><div class="star"></div><div class="star"></div></div>
        <p class="testimonial-text">The redesign improved trust and made the entire store feel more polished and credible.</p>
        <div class="testimonial-author">
          <div class="author-avatar">PP</div>
          <div>
            <div class="author-name">Priya Patel</div>
            <div class="author-loc">Ahmedabad</div>
          </div>
        </div>
      </div>

      <div class="testimonial-card">
        <div class="stars"><div class="star"></div><div class="star"></div><div class="star"></div><div class="star"></div><div class="star"></div></div>
        <p class="testimonial-text">Quick delivery, transparent process and reliable post-launch support. Exactly what we needed.</p>
        <div class="testimonial-author">
          <div class="author-avatar">AG</div>
          <div>
            <div class="author-name">Aman Gupta</div>
            <div class="author-loc">Delhi</div>
          </div>
        </div>
      </div>

    </div>
  </div>
</section>

<!-- FAQ -->
<section>
  <div class="container">
    <p class="section-eyebrow">FAQ</p>
    <h2 class="section-title">Frequently Asked Questions</h2>
    <p class="section-sub">Everything you need to know before we start working together.</p>
    <div class="faq-list">
      <div class="faq-item">
        <span class="faq-question">Do you only work with Shopify?</span>
        <span class="faq-arrow">→</span>
      </div>
      <div class="faq-item">
        <span class="faq-question">Can you redesign an existing store?</span>
        <span class="faq-arrow">→</span>
      </div>
      <div class="faq-item">
        <span class="faq-question">Do you provide support after launch?</span>
        <span class="faq-arrow">→</span>
      </div>
      <div class="faq-item">
        <span class="faq-question">Do you handle ongoing store management?</span>
        <span class="faq-arrow">→</span>
      </div>
      <div class="faq-item">
        <span class="faq-question">Can you build custom functionality?</span>
        <span class="faq-arrow">→</span>
      </div>
      <div class="faq-item">
        <span class="faq-question">How long does a typical project take?</span>
        <span class="faq-arrow">→</span>
      </div>
    </div>
  </div>
</section>

<!-- TRANSPARENCY -->
<section class="transparency-section">
  <div class="container">
    <p class="section-eyebrow">Our Commitments</p>
    <h2 class="section-title">What We Don't Do</h2>
    <p class="section-sub">Transparency is a core part of how we work. Here's what you'll never experience with us.</p>
    <div class="transparency-grid">
      <div class="transparency-item">We don't promise unrealistic revenue numbers or overnight results.</div>
      <div class="transparency-item">We don't disappear after launch. Post-launch support is part of the deal.</div>
      <div class="transparency-item">We don't overload stores with unnecessary apps that slow things down.</div>
      <div class="transparency-item">We don't outsource projects without your knowledge or full transparency.</div>
    </div>
  </div>
</section>

<!-- FREE AUDIT -->
<div class="audit-section" id="contact">
  <div class="audit-inner">
    <p class="section-eyebrow" style="margin-bottom: 16px;">Free Shopify Audit</p>
    <h2 class="audit-title">Get A Free Store Audit</h2>
    <p class="audit-desc">We'll identify design flaws, trust issues, speed bottlenecks, and conversion blockers — no strings attached.</p>
    <a href="mailto:hello@shopifygs.com" class="btn-primary" style="font-size: 15px; padding: 14px 36px;">Request Your Free Audit →</a>
  </div>
</div>

<!-- FUTURE VISION -->
<section>
  <div class="container">
    <p class="section-eyebrow">Future Vision</p>
    <h2 class="section-title">Growing Beyond Shopify</h2>
    <div class="vision-card">
      <div>
        <div class="vision-title">Expanding Into Custom E-Commerce</div>
        <p class="vision-text">Today we focus exclusively on Shopify. In the future we plan to expand into fully custom ecommerce platforms, SaaS products, and business websites — while maintaining the same quality standards that have earned us 200+ successful projects.</p>
      </div>
      <div class="vision-badge">
        <div class="vision-badge-title">Coming Soon</div>
        <div class="vision-badge-text">Custom Platforms</div>
      </div>
    </div>
  </div>
</section>

<!-- FINAL CTA -->
<section class="final-cta">
  <h2 class="final-cta-title">Ready To Build<br>Your Shopify Store?</h2>
  <p class="final-cta-sub">Let's create a store that looks premium and performs even better.</p>
  <div style="display: flex; gap: 16px; justify-content: center; flex-wrap: wrap;">
    <a href="mailto:hello@shopifygs.com" class="btn-primary" style="font-size: 16px; padding: 16px 40px;">Start Your Project →</a>
    <a href="#work" class="btn-ghost" style="font-size: 16px; padding: 16px 40px;">View Our Work</a>
  </div>
</section>

<!-- FOOTER -->
<footer>
  <div class="footer-inner">
    <div>
      <div class="footer-brand-name">Shopify<span>GS</span></div>
      <p class="footer-tagline">Premium Shopify design, development and store management for growing brands.</p>
    </div>
    <div>
      <div class="footer-col-title">Navigation</div>
      <ul class="footer-links">
        <li><a href="#work">Portfolio</a></li>
        <li><a href="#services">Services</a></li>
        <li><a href="#process">Process</a></li>
        <li><a href="#team">Team</a></li>
        <li><a href="#reviews">Reviews</a></li>
      </ul>
    </div>
    <div>
      <div class="footer-col-title">Services</div>
      <ul class="footer-links">
        <li><a href="#">Store Design</a></li>
        <li><a href="#">Development</a></li>
        <li><a href="#">Store Management</a></li>
        <li><a href="#">Speed Optimization</a></li>
        <li><a href="#">Migration</a></li>
      </ul>
    </div>
    <div>
      <div class="footer-col-title">Contact</div>
      <ul class="footer-links">
        <li><a href="mailto:hello@shopifygs.com">hello@shopifygs.com</a></li>
        <li><a href="#">WhatsApp</a></li>
        <li><a href="#">Instagram</a></li>
        <li><a href="#">LinkedIn</a></li>
      </ul>
    </div>
  </div>
  <div class="footer-bottom">
    <span class="footer-copy">© 2025 MAKE DIGITAL. All rights reserved.</span>
    <ul class="footer-socials">
      <li><a href="#">Privacy Policy</a></li>
      <li><a href="#">Terms</a></li>
    </ul>
  </div>
</footer>

</body>
</html>
